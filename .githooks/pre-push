#!/usr/bin/env sh
# Gate pushes on undeclared/unused checks (ESLint) and TypeScript files (tsc).
# Enable once per clone: git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"

if command -v bun >/dev/null 2>&1; then
  bun run check
else
  npm run check
fi
