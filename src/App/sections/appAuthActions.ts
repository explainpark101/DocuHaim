/**
 * §2 Auth Actions — unlock/lock, creds import/export, settings close guard.
 * Implementation: `useMainAppController.ts` (marker `// 2. Auth Actions`).
 */
export const APP_AUTH_ACTIONS_SECTION = '§2 Auth Actions' as const;
