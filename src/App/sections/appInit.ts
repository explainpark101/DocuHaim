/**
 * §1 Init — session restore / share-gate bootstrap.
 * Implementation currently lives in `useMainAppController.ts` (marker `// 1. Init`).
 * Phase C will promote this region into AppBootstrapProvider.
 */
export const APP_INIT_SECTION = '§1 Init' as const;
