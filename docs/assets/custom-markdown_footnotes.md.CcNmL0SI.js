import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.mOeOKQKb.js";var i=JSON.parse(`{"title":"Footnotes / Sources ([^1])","description":"","frontmatter":{},"headers":[],"relativePath":"custom-markdown/footnotes.md","filePath":"custom-markdown/footnotes.md"}`),a={name:`custom-markdown/footnotes.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="footnotes-sources-1" tabindex="-1">Footnotes / Sources (<code>[^1]</code>) <a class="header-anchor" href="#footnotes-sources-1" aria-label="Permalink to “Footnotes / Sources ([^1])”">​</a></h1><p>Numeric source footnotes. Body <code>[^N]</code> becomes an in-preview link; matching definitions at the <strong>bottom</strong> of the note render as a <strong>Sources</strong> list.</p><h2 id="per-document-enable-json-comment" tabindex="-1">Per-document enable (JSON comment) <a class="header-anchor" href="#per-document-enable-json-comment" aria-label="Permalink to “Per-document enable (JSON comment)”">​</a></h2><p>Place <strong>below</strong> optional <code>&lt;!-- note-cover … --&gt;</code> at the document top:</p><div class="language-html"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">&lt;!-- note-cover</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">{…}</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">--&gt;</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">&lt;!-- footnotes</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">{&quot;v&quot;:1,&quot;enabled&quot;:true}</span></span>
<span class="line"><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">--&gt;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>v</code></td><td><code>1</code></td><td><code>1</code></td><td>Schema version</td></tr><tr><td><code>enabled</code></td><td>boolean</td><td><code>true</code></td><td>When <code>false</code>, no body linking / no Sources block</td></tr></tbody></table><p>If the comment is omitted, footnotes stay <strong>enabled</strong> (compatible with existing notes).</p><p><code>--</code> inside JSON must be escaped as <code>\\u002d\\u002d</code> (same rule as note-cover).</p><h2 id="syntax-body-sources" tabindex="-1">Syntax (body + sources) <a class="header-anchor" href="#syntax-body-sources" aria-label="Permalink to “Syntax (body + sources)”">​</a></h2><div class="language-markdown"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Claim[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]and more[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]text.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: docs.docker.com - Define services in Docker Compose</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://docs.docker.com/reference/compose-file/services/</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-light-text-decoration:underline;--shiki-dark:#DBEDFF;--shiki-dark-text-decoration:underline;">^2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: github.com - Example issue</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">https://github.com/example/repo/issues/1</span></span></code></pre></div><ul><li><strong>Ref (body):</strong> <code>[^</code> + digits + <code>]</code> — no surrounding whitespace required</li><li><strong>Definition (trailing only):</strong> each entry is <code>[^N]:</code> (title) plus immediately following non-blank continuation lines (URL / extra text). <strong>Blank lines may separate entries.</strong></li><li>Unknown refs (no bottom definition) stay as plain text</li></ul><h2 id="global-display-mode" tabindex="-1">Global display mode <a class="header-anchor" href="#global-display-mode" aria-label="Permalink to “Global display mode”">​</a></h2><p>Settings → 마크다운 에디터 → <strong>각주 표기 방식</strong> (also Advanced Search):</p><table tabindex="0"><thead><tr><th>Mode</th><th>Preview</th></tr></thead><tbody><tr><td><code>sup</code> (default)</td><td><code>[^N]</code> in <code>&lt;sup&gt;</code></td></tr><tr><td><code>sub</code></td><td><code>[^N]</code> in <code>&lt;sub&gt;</code></td></tr><tr><td><code>rawText</code></td><td><code>[^N]</code> as normal-weight inline text</td></tr></tbody></table><p>Body always shows the literal form <code>[^N]</code> (not <code>[N]</code> / <code>^N</code>).</p><h2 id="insert-advanced-search" tabindex="-1">Insert (Advanced Search) <a class="header-anchor" href="#insert-advanced-search" aria-label="Permalink to “Insert (Advanced Search)”">​</a></h2><p>With the markdown editor open, Cmd/Ctrl+K → <strong>각주 삽입</strong> (always listed above other footnote commands):</p><ol><li><strong>기존 각주 선택</strong> — pick a trailing <code>[^N]</code> source; inserts that ref at the caret</li><li><strong>직접 각주 내용 입력</strong> — two-line modal (title + URL). Ctrl/Cmd+Enter inserts <code>[^N]</code> at the caret and appends the source block</li></ol><h2 id="spec-interop" tabindex="-1">Spec (interop) <a class="header-anchor" href="#spec-interop" aria-label="Permalink to “Spec (interop)”">​</a></h2><p>Implementation: <code>noteFootnotesMeta.ts</code>, <code>footnoteMarkdownIt.ts</code>, <code>previewFootnotesSettings.ts</code>.</p><h3 id="_1-grammar" tabindex="-1">1. Grammar <a class="header-anchor" href="#_1-grammar" aria-label="Permalink to “1. Grammar”">​</a></h3><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>FOOTNOTE_REF  := &#39;[&#39; &#39;^&#39; DIGITS+ &#39;]&#39;</span></span>
<span class="line"><span>SOURCE_LINE   := FOOTNOTE_REF ( &#39;:&#39; SP* )? REST_OF_LINE</span></span>
<span class="line"><span>DIGITS+       := /[0-9]+/</span></span>
<span class="line"><span>META_COMMENT  := &#39;&lt;!--&#39; WS &#39;footnotes&#39; WS JSON &#39;--&gt;&#39;</span></span></code></pre></div><h3 id="_2-parse-algorithm" tabindex="-1">2. Parse algorithm <a class="header-anchor" href="#_2-parse-algorithm" aria-label="Permalink to “2. Parse algorithm”">​</a></h3><ol start="0"><li><strong>Priority:</strong> footnotes win over CommonMark link-reference definitions. The built-in <code>reference</code> block rule skips <code>[^N]: …</code> (those stay footnote sources). Body <code>[^N]</code> is claimed before the inline <code>link</code> rule. Other <code>[label]: url</code> / <code>[label]</code> references stay CommonMark. After block parse, scrub leftover <code>env.references</code> keys matching <code>^N</code> (do not scrub bare <code>N</code>, so <code>[1]: url</code> still works).</li><li>If a leading <code>&lt;!-- footnotes … --&gt;</code> exists (after optional note-cover / blanks), parse JSON; <code>enabled:false</code> disables the rest.</li><li>From the document end, collect trailing source <strong>entries</strong>. Each entry is a <code>SOURCE_LINE</code> header plus immediately following non-blank continuation lines (no blanks inside an entry). Blank lines between entries are allowed. Stop when the next non-blank group above has no header (that group is body).</li><li>Remove meta comment + sources block from the body before block parsing.</li><li>Replace each <code>FOOTNOTE_REF</code> (claim before built-in <code>link</code>) with a known label by an in-preview link token; unknown labels stay literal <code>[^N]</code> text (still claimed so reference-links cannot steal them).</li><li>After block parse, scrub leftover <code>env.references</code> keys matching <code>^N</code> (bare <code>N</code> and other labels stay for CommonMark).</li><li>Append Sources HTML (when enabled).</li></ol><h3 id="_3-canonical-html" tabindex="-1">3. Canonical HTML <a class="header-anchor" href="#_3-canonical-html" aria-label="Permalink to “3. Canonical HTML”">​</a></h3><table tabindex="0"><thead><tr><th>Part</th><th>HTML</th></tr></thead><tbody><tr><td>Inline ref</td><td><code>&lt;a href=&quot;#&quot; class=&quot;footnote-ref-link bg-transparent&quot; … data-md-footnote-title=&quot;…&quot; aria-label=&quot;각주 [^N]: …&quot;&gt;</code> + wrapped <code>[^N]</code> + <code>&lt;/a&gt;</code></td></tr><tr><td>Wrap</td><td><code>&lt;sup|sub class=&quot;footnote-ref bg-transparent&quot;&gt;</code> or <code>&lt;span class=&quot;footnote-ref footnote-ref--raw bg-transparent&quot;&gt;</code></td></tr><tr><td>Item</td><td><code>&lt;li id=&quot;source-N&quot; data-md-footnote-label=&quot;N&quot; class=&quot;footnote-item&quot;&gt;</code> + <code>&lt;span class=&quot;footnote-marker&quot;&gt;[^N]&lt;/span&gt;</code> + <code>&lt;p&gt;…&lt;/p&gt;</code></td></tr><tr><td>Backref</td><td><code>&lt;a href=&quot;#&quot; class=&quot;footnote-backref&quot; data-md-footnote-to=&quot;fnref-N&quot;&gt;↩︎&lt;/a&gt;</code></td></tr></tbody></table><p>Navigation uses <code>data-md-footnote-to</code> (not URL hash / path) so HashRouter and SPA path handlers do not steal the click. Clicking a body ref scrolls the matching source to the <strong>top</strong> of the preview pane (as far up as remaining content allows). Backrefs keep <code>nearest</code>.</p><p>Hover/focus on an inline ref shows a Radix Tooltip with the source <strong>title</strong> (first non-empty definition line). Do not use HTML <code>title=</code>.</p><h3 id="_4-non-goals" tabindex="-1">4. Non-goals <a class="header-anchor" href="#_4-non-goals" aria-label="Permalink to “4. Non-goals”">​</a></h3><ul><li>Mid-document <code>[^N]:</code> definitions</li><li>Non-numeric labels</li><li>Novel / TipTap footnote UI</li></ul><h2 id="implementation" tabindex="-1">Implementation <a class="header-anchor" href="#implementation" aria-label="Permalink to “Implementation”">​</a></h2><table tabindex="0"><thead><tr><th>Role</th><th>Path</th></tr></thead><tbody><tr><td>Doc meta</td><td><code>src/utils/noteFootnotesMeta.ts</code></td></tr><tr><td>markdown-it</td><td><code>src/utils/footnoteMarkdownIt.ts</code></td></tr><tr><td>Display mode</td><td><code>src/utils/previewFootnotesSettings.ts</code></td></tr><tr><td>Preview scroll</td><td><code>src/utils/previewFootnoteScroll.ts</code></td></tr><tr><td>Preview tooltip</td><td><code>src/components/PreviewFootnoteTooltips.tsx</code></td></tr><tr><td>Global register</td><td><code>src/config/mdEditorConfig.js</code></td></tr><tr><td>Styles</td><td><code>src/styles/md-editor-rt/footnotes.css</code></td></tr><tr><td>AS insert</td><td><code>src/utils/advancedSearch/footnoteInsert.ts</code>, <code>FootnoteComposeModal.tsx</code></td></tr></tbody></table>`,32)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};